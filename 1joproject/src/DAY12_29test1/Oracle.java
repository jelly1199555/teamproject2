package DAY12_29test1;

public class Oracle {
	
	//sql에 테이블작성
	
	/*DROP TABLE login_table;

	CREATE TABLE login_table (
	user_id VARCHAR2(20),
	login_dt DATE DEFAULT SYSDATE,
	memo VARCHAR2(50)
	);

	-- login시
	INSERT INTO login_table (user_id) VALUES ('yskim');

	-- logout시
	UPDATE login_table
	SET logout_dt = SYSDATE
	WHERE user_id = 'yskim';

	-- 조회는 생략

	-- 참고 ORACLE엔 없으면 insert 있으면 Update문이 있음. MERGE임.

	select *
	from login_table;*/

}
